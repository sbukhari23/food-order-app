# ── Dependencies ─────────────────────────────
node_modules
.pnp
.pnp.js
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# ── Build output ─────────────────────────────
.next
out
dist
dist-ssr
build
*.tsbuildinfo
.turbo
.vercel

# ── Env files (never commit secrets) ─────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
*.pem

# ── Logs ──────────────────────────────────────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Test / coverage ───────────────────────────
coverage
test-results
.nyc_output
*.lcov

# ── Editor / IDE ──────────────────────────────
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
*.swo

# ── OS files ──────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Local runtime/generated data ─────────────
backend/data/orders.json
*.local

# ── AI assistant / agent context files ───────
CLAUDE.md
AGENTS.md
.claude/
.cursor/
.cursorrules
.windsurfrules
.clinerules
.github/copilot-instructions.md
.aider*
.continue/
.codeium/

# ── Package manager lock debris (keep ONE lockfile, ignore the rest) ─
# Uncomment if you standardize on npm and want to ignore others:
# yarn.lock
# pnpm-lock.yaml

# ── Misc secrets / keys some setups drop at root ─
*.key
*.cert