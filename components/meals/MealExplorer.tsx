'use client';
import { useEffect, useState } from 'react';
import type { Meal } from '@/lib/types';
import MealCard from './MealCard';
import { Search } from 'lucide-react';

export default function MealExplorer(){const [meals,setMeals]=useState<Meal[]>([]);const [categories,setCategories]=useState<string[]>([]);const [search,setSearch]=useState('');const [category,setCategory]=useState('');const [page,setPage]=useState(1);const [pages,setPages]=useState(1);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{const timer=setTimeout(()=>{setLoading(true);setError('');fetch(`/api/meals?search=${encodeURIComponent(search)}&category=${encodeURIComponent(category)}&page=${page}`).then(async(response)=>{if(!response.ok)throw new Error();return response.json();}).then((data)=>{setMeals(data.meals);setCategories(data.categories);setPages(data.pages);}).catch(()=>setError('The menu could not be loaded right now.')).finally(()=>setLoading(false));},250);return()=>clearTimeout(timer)},[search,category,page]);return <section id="menu" className="shell"><div className="toolbar"><label className="input search"><Search size={17} style={{verticalAlign:'middle',marginRight:8}}/><input value={search} onChange={(event)=>{setSearch(event.target.value);setPage(1)}} placeholder="Search the menu" aria-label="Search the menu" style={{border:0,outline:0,background:'transparent',color:'inherit',width:'calc(100% - 30px)'}}/></label><div className="chips"><button className={`chip ${!category?'active':''}`} onClick={()=>{setCategory('');setPage(1)}}>All</button>{categories.map((item)=><button className={`chip ${category===item?'active':''}`} key={item} onClick={()=>{setCategory(item);setPage(1)}}>{item}</button>)}</div></div>{loading?<div className="grid">{Array.from({length:8}).map((_,index)=><div className="card" style={{minHeight:360,opacity:.45}} key={index}/>)}</div>:error?<div className="notice"><p>{error}</p><button className="button" onClick={()=>setPage(page)}>Retry</button></div>:meals.length===0?<div className="notice"><h3>Nothing matched that craving.</h3><p>Try a broader search or clear the category filter.</p></div>:<><div className="grid">{meals.map((meal)=><MealCard meal={meal} key={meal.id}/>)}</div><div className="toolbar" style={{justifyContent:'center'}}><button className="button secondary" disabled={page<=1} onClick={()=>setPage(page-1)}>Previous</button><span className="muted">Page {page} of {pages}</span><button className="button secondary" disabled={page>=pages} onClick={()=>setPage(page+1)}>Next</button></div></>}</section>;}
